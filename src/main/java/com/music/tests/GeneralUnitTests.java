package com.music.tests;

public class GeneralUnitTests {

//    Adding a Song to an Artist
//    Artist artist = artistRepository.findById(artistId).orElseThrow();
//    Song song = new Song();
//    song.setTitle("New Song");
//    song.setArtist(artist);
//    artist.addSong(song);
//    songRepository.save(song);

//    Adding an Album to an Artist
//
//    Artist artist = artistRepository.findById(artistId).orElseThrow();
//    Album album = new Album();
//    album.setTitle("New Album");
//    album.setArtist(artist);
//    artist.addAlbum(album);
//    albumRepository.save(album);

//    Adding a Favorite Song to a User
//
//    User user = userRepository.findById(userId).orElseThrow();
//    Song song = songRepository.findById(songId).orElseThrow();
//    user.addFavoriteSong(song);
//    userRepository.save(user);

//    Adding a Favorite Album to a User
//    User user = userRepository.findById(userId).orElseThrow();
//    Album album = albumRepository.findById(albumId).orElseThrow();
//    user.addFavoriteAlbum(album);
//    userRepository.save(user);

}
